---
# Build and release WASM
# Only triggered on tags
name: Build and Release WASM
on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  FF_USE_FASTZIP: "true"
  ARTIFACT_COMPRESSION_LEVEL: "fast"

jobs:
  build:
    name: Build WASM
    runs-on: ubuntu-latest
    container: rust:latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y build-essential
          rustup default stable
          rustup target add wasm32-unknown-unknown
          # Pinned, not floating. ic-wasm dropped the `wasm-opt` feature in
          # 0.11.0, which removed the `optimize` subcommand that build.sh calls,
          # so an unpinned install fails the release with
          # "error: unexpected argument 'optimize' found".
          cargo install ic-wasm --version 0.9.10 --locked
          cargo install candid-extractor --version 0.1.6 --locked

      - name: Build
        run: |
          bash ./scripts/build.sh

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wasm-build
          path: wasm

  deploy:
    name: Release WASM
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: wasm-build
          path: wasm
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: wasm/*
